version: 0.2
phases:
    pre_build:
      commands:
        - echo "Enter prebuild phase"
    build:
      commands:
        - echo "Enter build phase"
    post_build:
      commands:
        - echo  "Enter post build phase."
        - npm install
artifacts:
  files:
    - ./restful-booker/**/*

