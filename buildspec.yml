version: 0.2
phases:
  install:
    runtime-versions:
      nodejs:10
  pre_build:
    commands:
      - echo "Enter prebuild phase"
      - npm install
  build:
    commands:
      - echo "Enter build phase"
  post_build:
    commands:
      - echo  "Enter post build phase."

artifacts:
  files:
    - ./restful-booker/**/*

